{
  "mcpServers": {
    "argocd-mcp": {
      "command": "uvx",
      "args": [
        "argocd-mcp@latest",
        "stdio"
      ],
      "env": {
        "ARGOCD_INSECURE_SKIP_VERIFY": "true",
        "ARGOCD_BASE_URL": "<argocd_url>",
        "ARGOCD_API_TOKEN": "<argocd_token>"
      }
    }
  }
}
